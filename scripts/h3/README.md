# scripts/h3 — cross-site 평가 (H3)

동결 모델·τ로 **B를 처음 개봉**해 A→B 일반화 직접 측정. B는 채점에만(누수 금지).

| 스크립트 | 한 일 |
|---|---|
| `h3b_crosssite.py` | cross-site 채점 A→B (★ B 개봉) |
| `h3c_mask_check.py` | 마스크 누수 검사(전이성) — 마스크 OFF 정당화 |

실행: `uv run python -m scripts.h3.h3b_crosssite`
