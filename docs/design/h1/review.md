# 검토 결과 — H1 (레드팀)

## 재검토 v4 — 회귀 확인

- **검토일**: 2026-06-27
- **대상**: working tree `docs/design/h1/decisions.md` (**DDD v4**, 개정 이력 `v4 2026-06-27`), base HEAD `be3bcad`.
- **검토자**: Claude Code (레드팀 모드)
- **결론**: **전 항목 PASS — HOLD 없음.** v3 대비 변경(개정 이력 v4 항목)만 회귀 점검. v1~v3 검토 결과는 git history(`901c37f`·`989ee28`·`444b095`) 참조.
- **다음**: `docs/design/h1/handoff.md` 작성 단계로 진행 가능(코드는 핸드오프에서).

### 회귀 점검 3건

1. **v3 HOLD(결정 4(c) 단방향/우측패딩 미명시) — ✅ 해소.**
   `docs/design/h1/decisions.md:79`가 "패딩 무누수는 **단방향(causal, t는 1..t만 사용) + 우측 패딩**일 때만 성립, 양방향이면 미래정보+패딩이 섞여 loss 마스킹으로도 못 막음 → 본 프로젝트 GRU는 단방향·우측 패딩으로 고정"을 명문화. **자립적**(스모크 의존 안 함을 본문이 명시). 핸드오프 강제 항목 3(`:163`)으로 재차 박음. 명문화 충분.

2. **추세통계 [검증 필요] — ✅ 해소(새 출처 주장 없이).**
   `:109,119`가 추세 통계를 **`delta`·`range`·`variance`로 특정**하고 "slope는 verbatim 미확인이라 미채택". 결정적으로 등급을 **[우리 결정]("출처 의존 없는 표준 통계")**으로 둠 — 상위팀이 이 3종을 썼다는 *주장 자체를 안 함*. [확인됨]으로 올리지 않고 닫은 정직한 처리. 전부-NaN lookback 처리(`:119`)도 명시.

3. **새 모순 — ✅ 없음.**
   - 단방향/우측패딩 ↔ 결정 8 시퀀스 구성(`:141` "정렬 패딩") 정합, docs/research/01 스트리밍 기조와 정합. 트리 경로(결정 6)엔 방향성 무관.
   - 추세통계 특정 ↔ 결정 6·8 어디와도 충돌 없음.
   - 핸드오프 강제 3종(`:158-163`)은 review v3 새위험 1~3과 일치, 타 결정과 무모순.
   - *경미(모순 아님)*: `range`(max−min)는 이미 나열된 min·max와 파생 중복이나, 트리 피처로 명시 제공은 무해.

### 잔여(핸드오프 디테일, HOLD 아님)
- 결정 4 검토요청의 pack/pad·평가 마스킹·긴 시퀀스 BPTT는 핸드오프 강제 1·2(`:161-162`)로 흡수됨 — 핸드오프에서 assert로 박을 것.

전 결정(1~8 + 부속)은 v3에서 PASS, v4 변경이 그 PASS를 깨지 않음을 확인. **핸드오프로 진행.**
