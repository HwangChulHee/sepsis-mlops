# H3 설계결정문서 (DDD) — cross-site 평가 (B 개봉)

> **설계 근거**: H2 산출물(6조합 학습 모델·전처리통계·τ, MLflow) 위에서 **B를 처음 펼쳐** cross-site 채점. H2 결정은 [`h2_decisions.md`](h2_decisions.md)(완료), 결과는 [`reports/h2_results.md`].
> **워크플로우·출처등급**: [`WORKFLOW.md`](WORKFLOW.md). 검토(`h3_review.md`) 통과 후 핸드오프로.
> **개정 이력**
> - **v2 (2026-06-28)** — review `71e47c6`의 HOLD 2건 + 정정 반영
>   - HOLD 1: **피처셋을 B 성능으로 고르는 것은 selection-on-test**(결정 2 위반) → 부속결정 수정: B는 1회 참고 관찰, 피처셋 선택은 **A-val + H4 운영 신호**, B는 선택 기준 미진입. PASS #2에 명시.
>   - HOLD 2: 마스크 검증(H3-c)을 **H3 필수로 복귀**(최소 대표 1조합) — WORKFLOW §8·H1 결정 7 약속 이행. 판정 기준을 **"마스크 ON/OFF의 A-val→B gap 비교"**(전이성)로 재서술, 공정 통제(HP\*·seed 동일·입력채널만 차이·τ는 A-val) 명문화.
>   - 정정: 우승 0.36 "재현"→**"스케일 참조"**(우승팀 예측·은닉셋 없어 재현 비현실). 결정 2 강제 기법(frozen-only 함수+금지함수 grep/assert).
> - v1: 초안.

## 한 줄 요약

H2에서 A로 학습한 **6조합을 B(봉인 해제)로 채점**해 cross-site 일반화를 측정한다. **A 전처리통계·τ를 그대로 적용**(B 재튜닝 절대 금지), **B는 채점에만 1회 개봉**. 추가로 utility 구현을 공식 스크립트와 최종 대조하고, 마스크 누수 가설을 검증한다. **운영(H4)이 최우선**이므로 H3는 그 전 검증 단계.

## H3 범위 / 범위 외

| 범위 (H3가 하는 것) | 범위 외 |
|---|---|
| 6조합 A→B 채점(PR-AUC+utility), A-val→B gap | 양방향 B→A (보류) |
| utility 공식 스크립트 최종 대조 (우승점수는 스케일 참조) | 서빙·드리프트·재학습 (H4, 최우선) |
| 마스크 누수 검증(필수, ON 재학습→A→B gap 비교) | 피처셋 최종 확정 (A-val+H4, B 미사용) |
| ⏸ gap·순위역전 해석(사람) | |

---

## 결정 1: 채점 대상 — 6조합 전부 A→B

- **결정**: H2의 **6조합(XGB·LGBM·GRU × vitals·vitals_labs)을 모두** B로 채점. 추론은 빠르므로 골라낼 이유 없음. PR-AUC + utility, **A-val→B gap**을 한 표로.
- **근거 + 출처등급**: 6조합 다 봐야 모델별·피처셋별 gap·순위역전을 완전 비교 [우리 결정]. H2 결과는 in-site라 본 시험대는 cross-site [확인됨: reports/h2_results.md, GRU A-val 0.40은 도메인 시프트 없음].
- **고려한 대안**: 대표만(GRU·XGBoost) 채점 → 피처셋·LGBM 비교 누락.
- **검토 요청 항목**: 6조합 모델·전처리통계·τ가 MLflow에서 정확히 로드되는지.

---

## 결정 2: 누수 규칙 — A 전처리·τ 그대로, B 재튜닝 금지, 1회 개봉 (★H3 핵심)

- **결정**: B 채점은 **A에서 산출·동결한 것만** 사용 — 정규화 μ/σ, fill 평균, 클리핑 범위, **τ** 전부 A(H2 저장 아티팩트). **B로 어떤 것도 재튜닝·재선정 금지**(τ 재탐색·정규화 재계산 모두 금지). **B는 채점에만 사용**, 모델 선택·하이퍼파라미터에 영향 줄 수 없음.
- **근거 + 출처등급**:
  - 실전(처음 보는 환자)은 과거(A) 기준으로 처리 — B 통계로 정규화하면 누수 [확인됨: ML 일반 / H1·H2 누수 원칙].
  - B를 한 번 보고 재튜닝하면 "시험지→연습지" 타락, cross-site 자기기만 [우리 결정 — 프로젝트 핵심 철학].
- **고려한 대안**: B에서 τ 재선정(점수는 오르나 누수 — 기각). B 통계로 정규화(누수 — 기각).
- **검토 요청 항목**: B가 전처리통계 산출·τ선정·모델선택에 닿는 경로가 정적·동적으로 차단되는지(동적 B-guard 역방향 — 이제 B가 *입력*이라 "B는 채점 함수에만, 학습/선정 함수엔 미진입"). **강제 기법**: 채점은 frozen-only 함수(A 아티팩트만 인자)로 격리하고, 금지 함수(fit·tune·select·정규화통계 산출)에 B가 전달되지 않음을 grep/assert로 차단.

---

## 결정 3: 평가 지표·gap — H2와 동일 잣대

- **결정**: PR-AUC(GRU masked) + utility, H2와 **동일 구현**. 보고는 **A-val → B 나란히 + gap = A_val − B**. 순위도 A/B 각각.
- **근거 + 출처등급**: 동일 잣대라야 gap이 의미 [우리 결정]. gap·순위역전이 핵심 발견(복잡한 GRU가 더 무너지나) [확인됨: cross-site 난제 — research/01].
- **고려한 대안**: B에서 새 지표 도입(비교 불가).
- **검토 요청 항목**: A-val 점수를 reports/h2_results.md에서 그대로 가져와 일관 비교하는지.

---

## 결정 4: utility 정밀 검증 — 공식 스크립트 최종 대조

- **결정**: B 채점 **전에** 우리 `eval/utility.py`를 **공식 `evaluate_sepsis_score.py`와 실제 데이터로 끝단 대조**(H2-a는 14행 표 검증, 여기선 공식 코드 자체와 동등성: 동일 입력→동일 출력 ±tol). 우승팀 ~0.36은 **스케일 참조**로만 인용(우승팀 예측·은닉셋이 없어 "재현"은 비현실 — 정정).
- **근거 + 출처등급**: 채점 도구가 틀리면 B 점수 전부 거짓 → B 개봉 전 최종 확정 [우리 결정]. utility 정의는 이미 [확인됨: H2-a, 14행 표 일치].
- **고려한 대안**: H2-a 검증으로 충분하다 보고 생략 → 공식 스크립트 자체와 미대조 위험.
- **미결/옵션**: 공식 스크립트 입수 경로(PhysioNet 공개). 우승점수 분해는 데이터 가용 시.
- **검토 요청 항목**: 공식 스크립트와 동등성 판정 기준(동일 입력 → 동일 출력 ±tol).

---

## 결정 5: 마스크 누수 검증 — 마스크 ON 재학습 후 B 비교

- **결정**: 마스크 OFF(기본)와 **마스크 ON**을 비교 — **H3 필수 항목**(WORKFLOW §8·H1 결정 7이 "마스크 채택은 H3 A·B 검증과 함께"로 약속). 마스크는 GRU 입력 채널이라 **마스크 ON으로 재학습 필요**(input_dim F→2F). 최소 **대표 1조합**(GRU vitals)은 반드시 수행.
  - **판정 기준 = 마스크 ON/OFF의 A-val→B gap 비교**(전이성). 마스크 ON이 A-val에선 비슷하거나 좋은데 **B에서 더 무너지면(gap↑)** → 마스크가 site-specific 측정패턴을 외운 것 → **OFF가 맞다**는 증거. gap 차이 미미하면 OFF 결정 영향 없음 확정. *(단순 "B 점수 오르내림"이 아니라 gap 변화가 핵심 — research/04: 관찰-과정 피처가 cross-site 이식성 악화.)*
  - **공정 비교 통제**: ON/OFF가 **HP\*·seed 동일**, **입력 채널만 차이**(마스크 외 전부 동일), **τ는 각자 A-val에서**. 그 외 전처리 동일.
- **근거 + 출처등급**: H1-c에서 측정밀도 누수 약함 확인(any-lab 1.05×, t0 직전 급증 없음) — 단 in-site → **cross-site 전이성은 H3에서 결판**(약속 이행) [확인됨: reports/h1_diagnostics.md, research/04]. 마스크 default-off/opt-in [확인됨: H1 결정 7].
- **고려한 대안**: H3-c 생략·H4 이연 → WORKFLOW §8·H1 약속 위반, 마스크 근거가 in-site에만 머묾. 기각.
- **미결/옵션**: 대표 1조합(GRU vitals) 필수, 추가 조합은 여유 시. 범위는 핸드오프.
- **검토 요청 항목**: ON 재학습이 OFF와 입력채널 외 동일 조건인지(HP·seed·전처리·τ방식), gap 비교가 프로그래매틱한지.

---

## 부속 결정

- **양방향(B→A) 보류**: A→B가 핵심 질문, B→A는 보강. **운영(H4) 최우선**이라 H3에선 A→B만 [우리 결정].
- **피처셋 최종 확정 이연**: H2에서 미결로 둔 메인 피처셋은 **A-val + H4 운영 신호(드리프트 견딤·실시간 성능 등)로 선택**한다. **B 점수로 고르지 않는다** — 2조합 중 B 높은 쪽을 고르면 selection-on-test로 그 B 점수가 정직한 held-out이 아니게 됨(결정 2 위반). **B는 "어느 게 더 잘 일반화하나" 1회 참고 관찰일 뿐, 선택 기준 미진입.**
- **사람 체크포인트(H3-b 후)** ⏸: A-val→B gap 표·순위역전을 보고 사람이 해석(GRU vs 트리 일반화, 피처셋 방향). 자동 진행 아님.

---

## H3 PASS 기준 (핸드오프에 박을 게이트 — 초안)

1. 6조합 B 채점 무오류 완주, PR-AUC+utility 기록.
2. **누수 가드**: B가 전처리통계·τ·**모델/피처셋 선택**에 미접촉(정적·동적), A 동결 아티팩트만 사용. 채점이 frozen-only 함수로 격리.
3. utility 공식 스크립트 동등성 통과(동일 입력→동일 출력 ±tol).
4. A-val→B gap 표 생성(H2 결과와 일관).
5. **마스크 ON/OFF B 비교 수치 산출(필수, 최소 GRU vitals)** — ON/OFF의 A-val→B gap 비교.

---

## 검토 상태

- v1(`71e47c6` 검토): HOLD 2건(featureset-on-B selection / 마스크 H3-c 이연) + 정정 → **본 v2에서 반영.**
- 1차 확인: 공식 스크립트 동등성 가능 [확인됨], 아티팩트 충분 [확인됨], 마스크=입력채널 재학습 필요 [확인됨].
- 다음: v2 재검토 → PASS 시 `h3_handoff.md`.